# bitcoinNotificationWidget

Simple little script to track bitcoin price getting notifications, can be set through a cron job to keep notifying at a suited time. Currently set to track KrakenEUR price.
